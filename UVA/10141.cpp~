#include <cstdio>

int main(void){
  char junk[MAX];
  
  for(int i = 1; scanf("%d %d", &n, &p), n != 0 && p != 0; i++){
    printf("RFP #%d\n", i);
    while(n--) fgets(junk, MAX, stdin);
  }
  }
  return 0;
}
