#include <stdio.h>
#include <string.h>
#define MAX 112345
int main(){
	int L;
	char dir[5],dobras[MAX][5],fio[MAX][5];
	scanf("%d",&L);
	while(L){
		for(int i=L-1; i>=1  ;i--){
			scanf("%s",dobras[i]);
			strcpy(wire[i],"+x");
		}
		for(int i=L-1 ; i >=1 ; i--){
			if(!strcmp(dobras[i],"+x"){
				
			}
		}
		
		scanf("%d",&L);
	}
	
	return 0;
}
