#include<stdio.h>
    int main()  {
        int x,num=1;
        scanf("%d",&x);
        if (x<=1000 && x>=1);
        {while (num<=x)
           {printf("%d\n",num);
        num+=2;}
        }
    return 0;}
