#include <cstdio>

#define MAX

double memo[MAXN][MAXV];

double pd(int i, int j){
  if(memo[i][j] != -1) return memo[i][j];
  
}

int main(void){
  while(scanf("%d %d", &m, &n) != EOF){
    ans = pd(m, n);
    if
  }
  
  return 0;
}
